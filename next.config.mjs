/** @type {import('next').NextConfig} */
const nextConfig = {

    images:{
        domains: ['miro.medium.com',"example.com"], // Add the domain(s) of your external image source
      }
};

export default nextConfig;
